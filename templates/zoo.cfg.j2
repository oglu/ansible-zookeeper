tickTime={{ tick_time }}
dataDir={{ data_dir }}
dataLogDir={{ log_dir }}
clientPort={{ zookeeper_client_port }}
initLimit={{ init_limit }}
syncLimit={{ sync_limit }}

{#
Also consider:
server.{{server.id}}={{server.host}}:2888:3888
#}

{% for server in groups["zookeepers"] %}
server.{{hostvars[server]['zoo_id']}} = {{server}}:2891:3888
{% endfor %}
